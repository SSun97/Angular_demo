import { SimonstyleDirective } from './simonstyle.directive';

describe('SimonstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SimonstyleDirective();
    expect(directive).toBeTruthy();
  });
});
